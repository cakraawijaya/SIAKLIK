$(document).ready(function() {

    // Modal reset password otomatis tampil saat load
    $('#modalResetPassword').modal({
        backdrop: 'static',
        keyboard: true,
        show: true
    });
    
});